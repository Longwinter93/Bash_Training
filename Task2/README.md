# Bash Training Task 2 
<br>We are going to practise these command:
<br>head / tail, grep, wc, cut, sort + uniq, awk, find, sed, perl, tee, touch, split, vi,  diff between exit and return, pipe, redirection, source command, cat
<br>Look for another bash commands for text processing, data enginner, data and data scientists 
<br>Reading about differences between text processing tools (sed, grep, awk, perl) - create a scripts to see these differences sed with while read: -> cat pattern.txt | while read; do sed -i "/$REPLY/d" input.txt; done
<br>Using these commands with while/for loop?

<br>Adding tree of file

+ Task1
 + Exercise1
 + Exercise2 
+ Task2
 + awk
