# Bash Training Task 2 
<br>We are going to practise these command:
<br>head / tail, grep, wc, cut, sort + uniq, awk, find, grep, sort, uniq, awk, and sed, tee,touch, head, tail, perl, split, diff between exit and return, pipe, redirection, source command, cat
