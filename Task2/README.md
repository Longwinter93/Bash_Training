# Bash Training Task 2 
<br>We are going to practise these command:
<br>head / tail, grep, wc, cut, sort + uniq, awk, find, sed, perl, tee, touch, split, vi, cat,tr , tar, zip  , pipe (a few examples with more commands), shell redirection (Changing the default path of input or output,stdin, stdout), source command,  more, less, bc , mv -to change the name of folder (using tr 'a-z' 'A-Z' < textsample.txt > uppertextsample.txt && mv uppertextsample.txt newuppertextsample.txt), rm, printf , mail bash command?
<br> Bash Command -  Diff, basename, ps, dirname, env,export,  vim, nano, watch, crontab , alias, history, man, ps, du, crontab,Try to use wget/curl to download sth from GitHub 
<br> how to create new files using bash command. 
<br>Learning about Linux distributions, Ubuntu, wsl (def, etc and install on Window etc). LEarning about basic related to Linux to understand it. Shebang in Linux
<br>Bash xargs with copy files to a folder and exec with find . Parameter expansion. Look at suck at programming - Array , IFS variable artihmetic expression and other things to be taken and practise ! Add this <- #look at suck at programming youtube to learn about it


<br> Process Monitoring (ps -ef, top, free -h, df -h, nproc, du), wget, curl, history, cron,crontab, less ,more 
<br>What is /dev/null?
<br>Reading about debugging and automation 
<br>browsing ListOfLinuxCommand and learn about ones that can be useful for me
<br>Look for another bash commands for text processing, data enginner, data and data scientists 
<br>shift and exit, curl, cut and tr with cat - look at suck at programming 
<br>defintion command line / def of all command bash using linuxize
<br>Adding tree of file in an inspiration.txt we have a good example of it (following directory tree)
<br>Adding explanations / definition about each command with README and with .sh files
+ Task1
 + Exercise1
 + Exercise2 
+ Task2
 + awk
