#!/bin/bash

#chmod (file permissions), chown (file ownership can be modifed for owner) chgrp (file ownership can be modifed for group) 
#Classes of user: Owner, Group, Others - users
#File permissions types: Read (r), write (w), execute (x)
ls -l